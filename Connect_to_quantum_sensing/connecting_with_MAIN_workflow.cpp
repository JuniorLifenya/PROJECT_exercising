

// After computing ground state
analyze_sensitivity(computed_frequencies, energy_shifts);

// In your visualization code
wigner_plot->SetTitle("NV-Center Strain Response;Strain (ε); Energy Shift (ΔE)");